let a = 1
let b = "1"

// Compare both values and types
if (a === b) {
  console.log("a and b are both equal and the same type")
}

// Compare types
if (a == b) {
  console.log("a and b are both 1")
}
